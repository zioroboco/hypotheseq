{
  "name": "hypotheseq",
  "version": "0.1.0-dev",
  "description": "Hypothes.is integrations for Logseq",
  "repository": {
    "type": "git",
    "url": "https://github.com/zioroboco/hypotheseq.git"
  },
  "license": "UNLICENSED",
  "private": true,
  "main": "src/index.html",
  "default": "src/index.html",
  "targets": {
    "main": false
  },
  "type": "module",
  "scripts": {
    "test": "pnpm install && NODE_OPTIONS='--no-warnings' mocha 'src/**/*.spec.js' -r dotenv/config -r cross-fetch/polyfill",
    "check": "pnpm install && tsc && dprint fmt"
  },
  "devDependencies": {
    "@logseq/libs": "^0.0.10",
    "@types/mocha": "^10.0.0",
    "cross-fetch": "^3.1.5",
    "dotenv": "^16.0.3",
    "dprint": "^0.32.2",
    "expect": "^29.3.1",
    "fetch-mock": "^9.11.0",
    "mocha": "^10.1.0",
    "typescript": "^4.8.4"
  },
  "logseq": {
    "id": "_qvr76awcr"
  }
}
